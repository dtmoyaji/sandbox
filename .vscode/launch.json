{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/nodejs/test.mjs",
            "cwd": "${workspaceFolder}/nodejs",
            "outFiles": ["${workspaceFolder}/out/**/*.js"]
        }
    ]
}